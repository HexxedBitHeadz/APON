#### Restart host
```command prompt - windows
shutdown /r /t 0
```

```command prompt - windows
shutdown.exe -r -f -t 1
```

Delay restart, exit pop up window.
```command prompt - windows
shutdown -r -t 10 && exit
```
